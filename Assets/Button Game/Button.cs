using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Button : MonoBehaviour
{
    //idea is to make this control the button state
    //replace button state from GameManager.cs
    //so GM.cs just pulls the state and controls gameplay/setup
    
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
